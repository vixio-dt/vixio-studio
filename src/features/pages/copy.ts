/** Every visible string in the pages view, per the build charter. */
export const pagesCopy = {
  empty: {
    title: "No pages yet",
    hint: "Pages hold your panel layouts. Add a page, pick a layout, then fill each panel in the panel lab.",
  },
} as const;
