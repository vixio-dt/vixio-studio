/** Every visible string in the previz view, per the build charter. */
export const previzCopy = {
  empty: {
    title: "No shots to block",
    hint: "Previz stages a shot in 3d before framing. Break scenes into shots on the board, then block the camera here.",
  },
} as const;
